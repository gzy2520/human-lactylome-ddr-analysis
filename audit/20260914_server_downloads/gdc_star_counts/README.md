# GDC STAR-count server-download snapshot

The isolated server job completed all 1,017 manifest UUIDs. Every status row is `complete`, the UUIDs are unique, and the observed MD5 equals the official manifest MD5 for every file.

The raw count files remain on the server. This snapshot contains only the final verification log and the server-generated README; it does not make the candidate cohorts analysis-ready.
